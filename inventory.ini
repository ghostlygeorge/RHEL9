[rhel9]
# Fill in per target host and the SSH key/user used to reach it.
# <host> ansible_ssh_user=<user> ansible_ssh_private_key_file=/path/to/key.pem

# Example (dummy values - replace before use):
# 203.0.113.10 ansible_ssh_user=admin ansible_ssh_private_key_file=~/.ssh/example-key.pem
